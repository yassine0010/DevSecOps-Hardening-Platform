sonar.projectKey=devSecOps   ← tells SonarQube WHICH project in its dashboard this scan belongs to
sonar.sources=.                        ← tells the scanner WHERE to look for code (repo root, in this case)