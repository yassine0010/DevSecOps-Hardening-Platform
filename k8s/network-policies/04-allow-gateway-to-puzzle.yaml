apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-gateway-to-puzzle
  labels:
    security: ingress-control
    component: puzzle-service
spec:
  # Target: puzzle-service pod
  podSelector:
    matchLabels:
      app: puzzle-service
  policyTypes:
    - Ingress
  ingress:
    # Only the NGINX Gateway Fabric data-plane pods may reach puzzle-service.
    # The gateway pods run in the same (default) namespace, so only a
    # podSelector is needed — no namespaceSelector required.
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: myapp-gateway-nginx
      ports:
        - protocol: TCP
          port: 3002
